In the process of passing the whole database (table) information to the front-
end for downloading, the information will be translated into a CSV (comma-
separated values) file before being sent as a file to the front-end.

The file will reside in this directory.