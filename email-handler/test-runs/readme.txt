You can do a test-run within this sub-package (i.e., in
`edapt-data-handler/email-handler`).  For example, while in the `email-handler`
directory in the shell you can execute the following:

`shell> node test-runs/test-run.js`